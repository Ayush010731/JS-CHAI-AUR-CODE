// array

const myArr = [0,1,2,3,4,5]

console.log(myArr[2]) //use to access element in the array

// Array methods

myArr.push(6); // it will push 6 in the array
myArr.pop(6); // it will pop last element of array

myArr.unshift(7); // it will insert 7 at the start of array


